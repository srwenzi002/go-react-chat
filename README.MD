# Go-React Chat 项目

## 启动数据库服务

要启动项目所需的数据库服务，请按照以下步骤操作：

```bash
cd .devcontainer/
chmod +x start-dbs.sh
./start-dbs.sh
```

这些命令将：
1. 切换到 `.devcontainer/` 目录
2. 为 `start-dbs.sh` 脚本添加执行权限
3. 执行数据库启动脚本